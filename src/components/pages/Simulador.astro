---
import Layout from "../../layouts/Layout.astro";
import Simualdor from "../Simulador/Simualdorintro.astro";
import SimuladorLogic from "../Simulador/SimuladorLogic.astro";

import { getI18N } from "../../i18n";

const { currentLocale } = Astro
const i18n = getI18N({ currentLocale })
---

<Layout
    title={ i18n.SIMULATOR.TITLE }
    description={i18n.SIMULATOR.DESCRIPTION }>
    <Simualdor />
    <SimuladorLogic />
</Layout>