---
import Layout from "../../layouts/Layout.astro";

import { getI18N } from "../../i18n";

const { currentLocale } = Astro
const i18n = getI18N({ currentLocale })
---

<Layout
    title={ i18n.SIMULATOR.TITLE }
    description={i18n.SIMULATOR.DESCRIPTION }>
</Layout>