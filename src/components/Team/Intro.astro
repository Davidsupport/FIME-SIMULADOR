---
import { getI18N } from "../../i18n";

const { currentLocale } = Astro;
const i18n = getI18N({ currentLocale });
---

<section>
    <div class="text-center text-3xl font-bold h-screen w-full">
        <h1>{i18n.TEAM.TITLE}</h1>
    </div>
</section>
