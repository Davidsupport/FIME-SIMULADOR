---
interface Props {
    checkActive?: boolean;
    href: string;
    class?: string;
}

const {
    href,
    class: className,
    checkActive = true,
    ...restOfProps
} = Astro.props;

const currentPath = Astro.url.pathname;
const isActive = currentPath === href && checkActive;
---

<a
    href={href}
    class:list={[
        className,
        "p-4 uppercase font-semibold",
        isActive ? "border-t-8 border-green-fime text-green-fime" : "border-transparent",
    ]}
    {...restOfProps}
>
    <slot />
</a>
